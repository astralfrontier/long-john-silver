{
  "scripts": {
    "start": "rustup toolchain install nightly && rustup target add wasm32-unknown-unknown && cargo install trunk && trunk build --release"
  },
  "dependencies": {
    "bulma": "^0.9.4"
  }
}
