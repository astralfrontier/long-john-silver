{
  "scripts": {
    "start": "rustup toolchain install nightly && cargo install trunk && trunk build --release",
    "build": "rustup toolchain install nightly && cargo install trunk && trunk build --release"
  },
  "dependencies": {
    "bulma": "^0.9.4"
  }
}
